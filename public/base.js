// document.addEventListener('DOMContentLoaded', function () {
//     // Définissez l'URL complète pour la connexion Socket.IO
//     const baseSocket = io('http://localhost:5000');

//     baseSocket.on('tessitureModification', function (data) {
//         console.log('Base Tessiture Modification:', data);
//         alert(`Tessiture modified for Base: ${data.candidats[0].nom} ${data.candidats[0].prenom}`);
//     });
// });
